# Python MongoDB CRUD Application

This is a simple Python console-based application that performs
CRUD (Create, Read, Update, Delete) operations using MongoDB.

## Tech Stack
- Python
- MongoDB
- PyMongo
- VS Code

  ## Setup Instructions
1. Install Python and MongoDB
2. Start MongoDB server
3. Create virtual environment
4. Install dependencies:
```bash
pip install -r requirements.txt

 ## Features
- Create user
- Read users
- Update user
- Delete user
- MongoDB integration using PyMongo
